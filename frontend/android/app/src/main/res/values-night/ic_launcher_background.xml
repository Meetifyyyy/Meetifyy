<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Dark-mode launcher icon background. Matches the app's dark surface
      (#202020, the colour of its header and bottom navigation) rather than pure
      black, so the icon reads as a tile on a dark home screen instead of a hole
      in it.
    -->
    <color name="ic_launcher_background">#202020</color>
</resources>
